echo [$(date)]: "START"
python -m venv env
source env/bin/activate
echo [$(date)]: "activate environment"
pip install --upgrade pip
pip install -r requirements.txt
echo [$(date)]: "creating environment with python $(python --version)"
echo [$(date)]: "END"

# run with "source init_setup.bh"

